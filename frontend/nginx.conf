server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # Proxy direct /estimate if needed (api routes usually prefixed, but checking existing routes)
    # Existing routes in backend: /api/services, /api/pricing, /estimate/ec2
    # Wait, backend/app/main.py:
    # app.include_router(services.router, prefix="/api")
    # app.include_router(pricing.router, prefix="/api/pricing")
    # app.include_router(estimate.router, prefix="/estimate")
    
    # We need to map /estimate too!

}
