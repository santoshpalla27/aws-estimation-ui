{
    "type": "object",
    "properties": {
        "source_servers": {
            "type": "integer",
            "title": "Source Servers",
            "default": 5,
            "minimum": 1
        },
        "replication_hours_per_month": {
            "type": "number",
            "title": "Replication Hours/Month",
            "default": 730,
            "minimum": 0
        }
    },
    "required": [
        "source_servers",
        "replication_hours_per_month"
    ]
}