{
    "type": "object",
    "properties": {
        "emails_sent_per_month": {
            "type": "integer",
            "title": "Emails Sent/Month",
            "default": 10000,
            "minimum": 0
        }
    },
    "required": [
        "emails_sent_per_month"
    ]
}