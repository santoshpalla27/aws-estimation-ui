{
    "type": "object",
    "properties": {
        "traces_recorded_per_month": {
            "type": "integer",
            "title": "Traces Recorded/Month",
            "default": 100000,
            "minimum": 0
        },
        "traces_retrieved_per_month": {
            "type": "integer",
            "title": "Traces Retrieved/Month",
            "default": 10000,
            "minimum": 0
        }
    },
    "required": [
        "traces_recorded_per_month",
        "traces_retrieved_per_month"
    ]
}