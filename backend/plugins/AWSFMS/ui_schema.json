{
    "type": "object",
    "properties": {
        "policies_per_region": {
            "type": "integer",
            "title": "Policies per Region",
            "default": 1,
            "minimum": 1
        }
    },
    "required": [
        "policies_per_region"
    ]
}