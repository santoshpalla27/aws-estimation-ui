{
    "type": "object",
    "properties": {
        "storage_gb": {
            "type": "number",
            "title": "Storage (GB)",
            "default": 10,
            "minimum": 0
        },
        "requests_per_month": {
            "type": "integer",
            "title": "Requests/Month",
            "default": 100000,
            "minimum": 0
        }
    },
    "required": [
        "storage_gb",
        "requests_per_month"
    ]
}