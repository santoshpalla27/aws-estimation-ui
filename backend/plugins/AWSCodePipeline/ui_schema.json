{
    "type": "object",
    "properties": {
        "active_pipelines": {
            "type": "integer",
            "title": "Active Pipelines",
            "default": 1,
            "minimum": 1
        },
        "pipeline_executions_per_month": {
            "type": "integer",
            "title": "Executions/Month",
            "default": 30,
            "minimum": 0
        }
    },
    "required": [
        "active_pipelines",
        "pipeline_executions_per_month"
    ]
}