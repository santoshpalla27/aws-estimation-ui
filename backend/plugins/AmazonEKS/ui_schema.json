{
    "type": "object",
    "properties": {
        "cluster_count": {
            "type": "integer",
            "title": "Number of Clusters",
            "default": 1,
            "minimum": 1
        },
        "node_group_type": {
            "type": "string",
            "title": "Node Group Type",
            "enum": [
                "EC2",
                "Fargate",
                "Both"
            ],
            "default": "Fargate"
        },
        "fargate_vcpu_hours": {
            "type": "number",
            "title": "Fargate vCPU Hours/Month",
            "default": 0,
            "minimum": 0
        },
        "fargate_gb_hours": {
            "type": "number",
            "title": "Fargate GB Hours/Month",
            "default": 0,
            "minimum": 0
        }
    },
    "required": [
        "cluster_count",
        "node_group_type"
    ]
}