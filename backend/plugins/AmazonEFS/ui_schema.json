{
    "type": "object",
    "properties": {
        "storage_class": {
            "type": "string",
            "title": "Storage Class",
            "enum": [
                "Standard",
                "Infrequent Access"
            ],
            "default": "Standard"
        },
        "storage_gb": {
            "type": "number",
            "title": "Storage (GB)",
            "default": 100,
            "minimum": 0
        }
    },
    "required": [
        "storage_class",
        "storage_gb"
    ]
}