{
    "type": "object",
    "properties": {
        "hosted_zones": {
            "type": "integer",
            "title": "Hosted Zones",
            "default": 1,
            "minimum": 1
        },
        "standard_queries_per_month": {
            "type": "integer",
            "title": "Standard Queries/Month",
            "default": 1000000,
            "minimum": 0
        },
        "health_checks": {
            "type": "integer",
            "title": "Health Checks",
            "default": 0,
            "minimum": 0
        }
    },
    "required": [
        "hosted_zones",
        "standard_queries_per_month"
    ]
}