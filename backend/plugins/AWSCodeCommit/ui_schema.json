{
    "type": "object",
    "properties": {
        "active_users": {
            "type": "integer",
            "title": "Active Users",
            "default": 5,
            "minimum": 1
        },
        "storage_gb": {
            "type": "number",
            "title": "Storage (GB)",
            "default": 1,
            "minimum": 0
        }
    },
    "required": [
        "active_users"
    ]
}