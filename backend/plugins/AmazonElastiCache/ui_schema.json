{
    "type": "object",
    "properties": {
        "engine": {
            "type": "string",
            "title": "Cache Engine",
            "enum": [
                "Redis",
                "Memcached"
            ],
            "default": "Redis"
        },
        "node_type": {
            "type": "string",
            "title": "Node Type",
            "enum": [
                "cache.t3.micro",
                "cache.t3.small",
                "cache.t3.medium",
                "cache.m5.large",
                "cache.m5.xlarge",
                "cache.r5.large"
            ],
            "default": "cache.t3.micro"
        },
        "number_of_nodes": {
            "type": "integer",
            "title": "Number of Nodes",
            "default": 1,
            "minimum": 1
        },
        "backup_storage_gb": {
            "type": "number",
            "title": "Backup Storage (GB)",
            "default": 0,
            "minimum": 0
        }
    },
    "required": [
        "engine",
        "node_type",
        "number_of_nodes"
    ]
}