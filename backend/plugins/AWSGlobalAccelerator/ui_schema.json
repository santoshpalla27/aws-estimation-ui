{
    "type": "object",
    "properties": {
        "accelerators": {
            "type": "integer",
            "title": "Number of Accelerators",
            "default": 1,
            "minimum": 1
        },
        "data_transfer_gb": {
            "type": "number",
            "title": "Data Transfer (GB)",
            "default": 100,
            "minimum": 0
        }
    },
    "required": [
        "accelerators",
        "data_transfer_gb"
    ]
}