{
    "type": "object",
    "properties": {
        "launch_type": {
            "type": "string",
            "title": "Launch Type",
            "enum": [
                "Fargate",
                "EC2"
            ],
            "default": "Fargate"
        },
        "vcpu_hours_per_month": {
            "type": "number",
            "title": "vCPU Hours/Month",
            "default": 730,
            "minimum": 0
        },
        "gb_hours_per_month": {
            "type": "number",
            "title": "GB Memory Hours/Month",
            "default": 1460,
            "minimum": 0
        },
        "storage_gb": {
            "type": "number",
            "title": "Ephemeral Storage (GB)",
            "default": 20,
            "minimum": 20
        }
    },
    "required": [
        "launch_type",
        "vcpu_hours_per_month",
        "gb_hours_per_month"
    ]
}