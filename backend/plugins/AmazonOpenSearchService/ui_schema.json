{
    "type": "object",
    "properties": {
        "instance_type": {
            "type": "string",
            "title": "Instance Type",
            "enum": [
                "t3.small.search",
                "t3.medium.search",
                "m5.large.search"
            ],
            "default": "t3.small.search"
        },
        "instance_count": {
            "type": "integer",
            "title": "Instance Count",
            "default": 2,
            "minimum": 1
        },
        "storage_gb": {
            "type": "number",
            "title": "Storage (GB)",
            "default": 100,
            "minimum": 0
        }
    },
    "required": [
        "instance_type",
        "instance_count",
        "storage_gb"
    ]
}