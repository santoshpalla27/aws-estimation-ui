{
    "type": "object",
    "properties": {
        "load_balancer_type": {
            "type": "string",
            "title": "Load Balancer Type",
            "enum": [
                "Network",
                "Classic"
            ],
            "default": "Network"
        },
        "number_of_lbs": {
            "type": "integer",
            "title": "Number of Load Balancers",
            "default": 1,
            "minimum": 1
        },
        "data_processed_gb": {
            "type": "number",
            "title": "Data Processed (GB)",
            "default": 1000,
            "minimum": 0
        }
    },
    "required": [
        "load_balancer_type",
        "number_of_lbs",
        "data_processed_gb"
    ]
}