{
    "type": "object",
    "properties": {
        "web_acls": {
            "type": "integer",
            "title": "Web ACLs",
            "default": 1,
            "minimum": 1
        },
        "rules_per_acl": {
            "type": "integer",
            "title": "Rules per ACL",
            "default": 5,
            "minimum": 1
        },
        "requests_per_month": {
            "type": "integer",
            "title": "Requests/Month",
            "default": 10000000,
            "minimum": 0
        }
    },
    "required": [
        "web_acls",
        "rules_per_acl",
        "requests_per_month"
    ]
}