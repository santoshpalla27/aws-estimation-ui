{
    "type": "object",
    "properties": {
        "number_of_secrets": {
            "type": "integer",
            "title": "Number of Secrets",
            "default": 10,
            "minimum": 0
        },
        "api_calls_per_month": {
            "type": "integer",
            "title": "API Calls/Month",
            "default": 10000,
            "minimum": 0
        }
    },
    "required": [
        "number_of_secrets",
        "api_calls_per_month"
    ]
}