# AWS Lambda Pricing Data
# Source: https://aws.amazon.com/lambda/pricing/
# Last Updated: 2024-12-15

pricing:
  regions:
    us-east-1:
      compute:
        gb_second: 0.0000166667      # $0.0000166667 per GB-second
        
      requests:
        per_million: 0.20            # $0.20 per 1M requests
        
      architecture_multiplier:
        x86_64: 1.0
        arm64: 0.8                   # 20% cheaper with Graviton2
      
      ephemeral_storage:
        per_gb_second: 0.0000000309  # Additional storage beyond 512MB
      
      provisioned_concurrency:
        per_vcpu_hour: 0.0000041667  # If using provisioned concurrency
        per_gb_hour: 0.0000041667
    
    us-west-2:
      compute:
        gb_second: 0.0000166667
        
      requests:
        per_million: 0.20
        
      architecture_multiplier:
        x86_64: 1.0
        arm64: 0.8
      
      ephemeral_storage:
        per_gb_second: 0.0000000309
      
      provisioned_concurrency:
        per_vcpu_hour: 0.0000041667
        per_gb_hour: 0.0000041667
    
    eu-west-1:
      compute:
        gb_second: 0.0000166667      # Same price globally
        
      requests:
        per_million: 0.20
        
      architecture_multiplier:
        x86_64: 1.0
        arm64: 0.8
      
      ephemeral_storage:
        per_gb_second: 0.0000000309
      
      provisioned_concurrency:
        per_vcpu_hour: 0.0000041667
        per_gb_hour: 0.0000041667
  
  last_updated: "2024-12-15"
  source: "AWS Lambda Pricing Page"
  currency: "USD"
  pricing_model: "on-demand"
  
  free_tier:
    enabled: true
    compute_gb_seconds: 400000       # 400,000 GB-seconds per month (always free)
    requests: 1000000                # 1M requests per month (always free)
    
    # Example: 128MB function running for 3.125M seconds = 400,000 GB-seconds
    # Example: 1GB function running for 400,000 seconds = 400,000 GB-seconds

assumptions:
  - "AWS Free Tier applies (400,000 GB-seconds and 1M requests per month)"
  - "No provisioned concurrency configured"
  - "Standard Lambda pricing (not Lambda@Edge)"
  - "Average duration used for all invocations"
  - "Ephemeral storage default is 512MB (included in base price)"
  - "Data transfer charges apply separately"
