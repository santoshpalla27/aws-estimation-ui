{
    "type": "object",
    "properties": {
        "queue_type": {
            "type": "string",
            "title": "Queue Type",
            "enum": [
                "Standard",
                "FIFO"
            ],
            "default": "Standard",
            "description": "Type of SQS queue"
        },
        "requests_per_month": {
            "type": "integer",
            "title": "Requests per Month",
            "default": 1000000,
            "minimum": 1,
            "description": "Send, receive, delete requests"
        },
        "average_message_size_kb": {
            "type": "number",
            "title": "Average Message Size (KB)",
            "default": 64,
            "minimum": 1,
            "maximum": 256,
            "description": "Average size of messages"
        }
    },
    "required": [
        "queue_type",
        "requests_per_month"
    ]
}